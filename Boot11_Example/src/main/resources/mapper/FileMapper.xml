<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="file">
	<sql id="column">
		num, writer, title, orgFileName, saveFileName, fileSize, regdate
	</sql>

	<select id="getList" resultType="com.example.boot11.dto.FileDto">
		SELECT <include refid="column"/>, NVL(MAX(ROWNUM), 0) as count
		FROM board_file
		GROUP BY <include refid="column"/>
		ORDER BY num ASC
	</select>
	
	<insert id="insert" parameterType="com.example.boot11.dto.FileDto">
		INSERT INTO board_file
		( <include refid="column"/>)
		VALUES (board_file_seq.NEXTVAL, #{writer}, #{title}, #{orgFileName}, 
		#{saveFileName}, #{fileSize}, SYSDATE)
	</insert>
	
	<select id="getData" parameterType="int" resultType="com.example.boot11.dto.FileDto">
		SELECT <include refid="column"/>
		FROM board_file
		WHERE num=#{num }
	</select>
	
	<delete id="delete" parameterType="int">
		DELETE FROM board_file
		WHERE num=#{num }
	</delete>
	
	<select id="getCount" resultType="int">
		SELECT NVL(MAX(ROWNUM), 0)
		FROM board_file
	</select>
</mapper>